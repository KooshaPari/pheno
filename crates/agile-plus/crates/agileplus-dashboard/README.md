# agileplus-dashboard

Dashboard server crate for HTML/htmx routes and dashboard health surfaces.

## Public API Index

- Modules: `app_state`, `health`, `process_detector`, `routes`, `seed`, `seed_bridge`, `templates`.
- Binary target starts the dashboard service.

## Validation

```bash
cargo test -p agileplus-dashboard
```

