# agileplus-domain

Core domain types, errors, configuration, credentials, and port traits.

## Public API Index

- Modules: `config`, `credentials`, `domain`, `error`, `ports`.
- Keep this crate pure: no framework, database, or transport dependencies.

## Validation

```bash
cargo test -p agileplus-domain
```

