[package]
name = "agileplus-mcp"
version = "0.1.0"
edition = "2021"
description = "AgilePlus MCP server (stub — model-context-protocol facade, see crates/agileplus-cli for impl)"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
