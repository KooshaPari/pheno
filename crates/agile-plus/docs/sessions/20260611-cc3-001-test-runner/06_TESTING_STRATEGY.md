# Testing Strategy

- Validate target discovery with `just --list`.
- Run `just test-unit`.
- Run `just test-integration`.
- If full integration execution is environment-dependent, capture the real command result in the worklog notes.
