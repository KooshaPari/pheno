# CODEOWNERS Template — Phenotype baseline
# Phase 3 Lane 4 / Batch 2 governance audit (2026-06-23).
#
# Use this template when bootstrapping a new Phenotype repo (or refreshing an
# existing one). Copy the body below into `./CODEOWNERS` at the repo root.
# GitHub auto-detects CODEOWNERS at `.github/CODEOWNERS` and `./CODEOWNERS`.
#
# Last reviewed in: docs/audit/batch2-governance-findings.md
# Reference baseline: KooshaPari/AgilePlus `CODEOWNERS` (19 lines, single-owner).

# ---- Single-owner baseline (Phenotype org) ----
# Replace @<owner-handle> before copying into a new repo.
/crates/   @<owner-handle>
/apps/     @<owner-handle>
/tools/    @<owner-handle>

# ---- Documentation ----
*.md       @<owner-handle>

# ---- CI / config (YAML) ----
*.yml      @<owner-handle>
*.yaml     @<owner-handle>

# ---- Default fallback ----
*          @<owner-handle>

# ---- Optional multi-owner variant (delete the block above if you use this) ----
# Uncomment + adapt when team grows beyond a single owner:
#
# /crates/agileplus-domain/   @KooshaPari @domain-maintainer
# /crates/agileplus-ports/    @KooshaPari @ports-maintainer
# /crates/agileplus-adapters/ @KooshaPari @adapters-maintainer
# /apps/                       @KooshaPari @apps-maintainer
# /tools/                      @KooshaPari @tools-maintainer
# /docs/                       @KooshaPari @docs-maintainer
# /specs/                      @KooshaPari @spec-maintainer
# *.md                         @KooshaPari @docs-maintainer
# *.yml                        @KooshaPari @platform-maintainer
# *.yaml                       @KooshaPari @platform-maintainer
# *                            @KooshaPari