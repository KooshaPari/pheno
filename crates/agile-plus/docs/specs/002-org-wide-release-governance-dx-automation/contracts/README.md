# docs/specs/002 — contracts

## Provenance

| File | Source | Maintenance |
|------|--------|-------------|
| `registry-adapter.md` | (design doc, no SSOT in `proto/`) | **Hand-maintained** |

## Note

This spec's contracts directory contains only narrative design documentation
(Markdown), not proto or other generated artifacts. It is **not** regenerated
from `proto/agileplus/v1/` by the `regen-docs-specs.yml` workflow.

Edit `registry-adapter.md` directly. See SSOT audit issue #121 step 3 for scope.
