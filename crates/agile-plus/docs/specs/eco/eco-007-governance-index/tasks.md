# Tasks: Governance Index

## WP-01: Live Governance Index

**File Scope:**
- Read: `AgilePlus/kitty-specs/*/meta.json`
- Write: `AgilePlus/tooling/governance_index.py`,
  `AgilePlus/kitty-specs/INDEX.md`,
  `.github/workflows/governance-index.yml`,
  `scripts/regen-index.sh`

**Depends on:** none
**Effort:** S

### Acceptance Criteria
- [ ] `INDEX.md` exists, lists every kitty-spec, and is regenerated by
      the indexer script.
- [ ] CI workflow regenerates `INDEX.md` on push to `main`.
- [ ] Local reproduction: `python3 tooling/governance_index.py` matches
      the committed `INDEX.md`.
- [ ] UTF-8 (no BOM) validation passes for `INDEX.md`.

### Tasks
- [ ] T001 — Implement `AgilePlus/tooling/governance_index.py`:
      walk `kitty-specs/*/meta.json`, sort by `spec_id`, emit a
      Markdown table with columns
      `spec_id | slug | status | created | reactivated |
      superseded_by | link`. Handle missing fields by rendering `—`.
- [ ] T002 — Generate the initial `AgilePlus/kitty-specs/INDEX.md`
      via the indexer and commit it.
- [ ] T003 — Add `.github/workflows/governance-index.yml` running on
      `ubuntu-latest`, triggered by `push` to `main` and
      `workflow_dispatch`. Steps: checkout, run indexer, commit any
      diff to `INDEX.md` and push.
- [ ] T004 — Add a `scripts/regen-index.sh` wrapper (or
      `Makefile` target) for local one-command regeneration that
      delegates to the same indexer.
- [ ] T005 — Validate UTF-8 (no BOM) for `INDEX.md` with
      `agileplus validate-encoding --all` and document the command
      in `tooling/governance_index.py` docstring.
- [ ] T006 — Re-run the `eco-006-compliance` audit and confirm FR1
      ("live governance index") checkbox resolves to "implemented";
      append evidence to
      `worklogs/eco-006-compliance-20260605.md` or a follow-up
      compliance entry.
