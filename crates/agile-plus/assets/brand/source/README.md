# Brand Icon Source

`icon.svg` is a placeholder source for the per-OS icon export pipeline.

TODO: Replace `icon.svg` with the real approved AgilePlus icon design before
using generated assets in production releases. The current file is intentionally
plain and marked as a placeholder so it is not mistaken for final art.
