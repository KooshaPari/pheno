# xtask-anti-patterns

Workspace linter for Cargo and repository anti-patterns.

## Public API Index

- Binary crate invoked with `cargo run -p xtask-anti-patterns`.
- Checks workspace-level anti-patterns that are easier to enforce outside normal crate code.

## Validation

```bash
cargo test -p xtask-anti-patterns
```

