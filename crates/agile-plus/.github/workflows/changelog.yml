name: Generate Changelog

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
        description: 'Version for changelog entry'
permissions:
  contents: read


jobs:
  changelog:
    runs-on: ubuntu-24.04
    env:
      VERSION: ${{ inputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install git-cliff
        run: |
          cargo install git-cliff

      - name: Generate changelog
        run: |
          git-cliff --tag "$VERSION" > CHANGELOG.md || echo "# Changelog ($VERSION) — release notes unavailable; see commit history" > CHANGELOG.md

      - name: Commit and push changelog
        continue-on-error: true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git diff --cached --quiet || git commit -m "chore: update changelog for $VERSION"
          git push

      - name: Changelog summary
        run: |
          echo "Changelog generated for version $VERSION"
