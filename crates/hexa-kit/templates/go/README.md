# template-lang-go

Go layer templates composed on top of template-commons.

## Layer Contract

- layer_type: language
- layer_name: template-lang-go
- versioning: semver

## Usage

1. Consume this layer via pinned version in downstream domain templates.
2. Run `task check` before release.
3. Publish version + changelog update for every contract-affecting change.
