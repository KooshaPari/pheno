name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate manifest and reconcile rules
        run: |
          test -f contracts/template.manifest.json
          test -f contracts/reconcile.rules.yaml
          bash scripts/scaffold-smoke.sh
