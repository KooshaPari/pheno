# Contracts

`template.manifest.json` describes the layer's metadata, generated files, and ownership. Update the version whenever you change the Mojo scaffold.

`reconcile.rules.yaml` declares reconciliation modes and ownership policies for this layer.
