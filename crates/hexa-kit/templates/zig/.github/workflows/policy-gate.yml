name: Policy Gate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  policy-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate repository contract
        run: |
          sudo apt-get update
          sudo apt-get install -y zig
          REQUIRE_ZIG=1 bash scripts/scaffold-smoke.sh
