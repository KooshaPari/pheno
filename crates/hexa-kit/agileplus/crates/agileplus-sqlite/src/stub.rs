//! Temporary stub for `agileplus-sqlite`.
//!
//! The full crate has a rich source tree in this directory (migrations,
//! repository modules, content storage / storage ports). That code is not
//! yet wired up — the dependencies (rusqlite, async-trait, thiserror, plus
//! the full `agileplus-domain` / `agileplus-events` public surface) are
//! not declared. This stub is the smallest possible crate that compiles
//! and lets the workspace graph resolve.
//!
//! To finish wiring up the real crate, see the comments in
//! `Cargo.toml` and follow the four-step migration there.

#![doc = "Stub — see Cargo.toml for the migration plan."]
