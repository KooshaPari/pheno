---
source: cursor
session_id: 00e8382f-d39d-47d7-ab46-f3725fac17d4
captured_at: 2026-06-17T02:26:13.440643+00:00
verbatim_hash: sha256:88044253cdaf8aeab1d2e2d8ab3c84136512d876c7bce397048ef8f1b8f36fdf
repository_context: HexaKit
transcript: C:\Users\koosh\.cursor\projects\C-Users-koosh\agent-transcripts\00e8382f-d39d-47d7-ab46-f3725fac17d4\00e8382f-d39d-47d7-ab46-f3725fac17d4.jsonl
turn: 1
---

## Verbatim user prompt

FULL CODE MIGRATION (not just docs) of KodeVibeGo into its successor, then archive. Reviewer agent, user sponsor-only, full merge+archive authority. CRITERION: archive only once the actual RUNTIME/PRODUCT CODE is migrated into the successor + builds green + verified 100% moved (ideas-only does NOT count). Hard rails: NEVER delete; NEVER touch P2/472-P2/WSM3D/Civis/DINOForge/BytePort. Work on E: (clone to E:/scratch/migrate/<repo>; per-repo CARGO_TARGET_DIR=E:/tm/<repo>). Read E:/scratch/mined-4-repos.md for the overlap map.

KodeVibeGo (active, Go; "vibe" static-analysis CLI/daemon: security/code/perf + MCP hooks). Successor = KodeVibe (Shell CLI) + HexaKit governance. HexaKit#152 already added the SCHEMA/registry/scoring DOCS — now migrate the actual CODE:
1. Clone KodeVibeGo. Identify its real runtime code (the Go static-analysis engine, daemon, MCP hooks, scoring impl).
2. Migrate it into the best successor: if KodeVibe is the canonical CLI, port KodeVibeGo's Go engine in as a module/subtree there (or as a HexaKit governance crate if that's the better home — judge by where it's consumed). git subtree add --squash the source tree into the successor under a sensible path, wire it to build, fix cheap breakages. Open PR, build-verify green, self-merge (admin).
3. VERIFY 100% moved: the static-analysis engine/daemon/MCP-hooks/scoring code now lives + builds in the successor; nothing of runtime value left only in KodeVibeGo. 
4. If verified → gh repo archive KooshaPari/KodeVibeGo -y. If a genuine blocker prevents full migration, migrate what you can, report exactly what remains, do NOT archive.
Report: what code migrated (PR#), builds green?, 100% moved verified?, archived? (or remainder). Authority is yours.
