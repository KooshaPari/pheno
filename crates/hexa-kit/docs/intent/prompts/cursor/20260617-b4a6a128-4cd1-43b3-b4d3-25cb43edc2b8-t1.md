---
source: cursor
session_id: b4a6a128-4cd1-43b3-b4d3-25cb43edc2b8
captured_at: 2026-06-17T02:26:14.519816+00:00
verbatim_hash: sha256:fdb8a638dc0a9befcbea9425cbd4af1a18a4c2a3d35fc3e2ddf3e9497d990d0d
repository_context: HexaKit
transcript: C:\Users\koosh\.cursor\projects\C-Users-koosh\agent-transcripts\b4a6a128-4cd1-43b3-b4d3-25cb43edc2b8\b4a6a128-4cd1-43b3-b4d3-25cb43edc2b8.jsonl
turn: 1
---

## Verbatim user prompt

DEEP read-only investigation of 6 repos to determine TRUE archive-eligibility by the CORRECT test: does the repo contain real CODE (across ALL BRANCHES, not just default), and has that code been 100% MIGRATED into a successor + verified? (The prior verify wrongly used a DEPENDENTS check — ignore that approach.) Archive NOTHING. NEVER touch P2/472-P2/WSM3D/Civis/DINOForge/BytePort. Work on E: (clone to E:/scratch/reserved/<repo>).

USER CONTEXT (critical):
- **pheno-sdk**: user says it's a 1M-3M LOC project (a prior agent saw "3 files" = WRONG clone/scan or a different repo). MUST clone properly + check ALL branches — likely huge unmoved code → NOT archivable.
- **Pine**: new, unresearched project → almost certainly KEEP (no successor to have moved to).
- **phenoXdd**: "was robust, needed merging" — determine if it was ACTUALLY merged into a successor (which? HexaKit? phenotype-dep-guard?). If not merged → not archivable.
- **Zerokit, Httpora**: check real code + whether moved.

For EACH (Pine, pheno-sdk, phenoXdd, Zerokit, Httpora — skip the 6th if it's already-handled; also include any of these 6 the user listed):
1. `gh repo view` + `git clone` (depth 1) + `git ls-remote --heads` to enumerate ALL branches. If default branch looks empty, CHECK OTHER BRANCHES (code may live on a feature/wip branch).
2. Measure real code: count source files (.rs/.py/.go/.ts/etc.) across branches, total LOC estimate, what it actually IS.
3. Determine if its code exists in any SUCCESSOR repo (search the likely successor for the same modules). Is it 100% moved? partially? not at all?
4. VERDICT per repo: KEEP-ACTIVE (real unmoved code / new project) | NEEDS-MIGRATION-THEN-ARCHIVE (has code, not moved — name the successor) | ARCHIVABLE (genuinely empty OR 100%-moved-verified).
Write to E:/scratch/reserved-slots-deepverify.md + print: repo | branches | real-LOC | what-it-is | moved? | VERDICT + reasoning. Be especially careful with pheno-sdk (expect large). Archive NOTHING — this is investigation only.
