---
source: cursor
session_id: d142000e-194f-4a58-8f8d-079822ac33a2
captured_at: 2026-06-17T02:26:14.601181+00:00
verbatim_hash: sha256:2acada4260e7cca165cde3bff67bc83b21b0895c900256c8df496a88c15a2ce9
repository_context: HexaKit
transcript: C:\Users\koosh\.cursor\projects\C-Users-koosh\agent-transcripts\d142000e-194f-4a58-8f8d-079822ac33a2\d142000e-194f-4a58-8f8d-079822ac33a2.jsonl
turn: 1
---

## Verbatim user prompt

BORROW-then-ARCHIVE for 2 superseded repos (KVirtualStage, KodeVibeGo). You are a reviewer+fixer agent; user is sponsor-only (you borrow→verify→merge→archive autonomously). ARCHIVE CRITERION: a source may be archived ONLY once 100% MOVED — all valuable content merged into successor + verified complete; not before. Hard rails: NEVER delete; NEVER touch P2/472-P2/WSM3D/Civis/DINOForge/BytePort. ALL on E: (clone to E:/scratch/borrow/<repo>; per-repo CARGO_TARGET_DIR=E:/t/<repo> — never share target dirs). Findings already in E:/scratch/mined-4-repos.md (read it).

1. **KVirtualStage** (archived, Go; successor = KDesktopVirt): unarchive is NOT needed — read its content (gh repo clone works per the mine doc). Extract the valuable patterns (ADRs, session-control API REST/GraphQL/WS/TUI, docker-sandbox design) into KDesktopVirt as an additive PR (docs/ADRs + any reusable Go). Self-merge if green. THEN verify KVirtualStage has nothing-of-value left un-moved; if 100% moved, it STAYS ARCHIVED (already is) — just note "fully superseded, archived". 
2. **KodeVibeGo** (active, Go, deprecated→HexaKit; successor = KodeVibe + HexaKit governance): extract its valuable bits (vibe static-analysis schema/registry/scoring, MCP hooks) into the successor as additive PR(s); self-merge if green. THEN verify 100% moved; if complete, `gh repo archive KooshaPari/KodeVibeGo -y` (reversible). If NOT fully moved, leave active + report what remains.
Report per repo: what was borrowed (PR#), 100%-moved verified?, archived? (or why not). Don't defer to user; you have merge+archive authority within the criterion.
