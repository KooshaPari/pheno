# UX — SOTA (HexaKit genesis)

**N/A** — HexaKit is developer infrastructure with no end-user UI.

End-user UX for products built *from* HexaKit templates is owned by each application repo's `docs/sota/ux.md`.
