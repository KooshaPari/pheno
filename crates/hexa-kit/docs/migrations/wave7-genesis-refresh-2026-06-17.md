# Wave 7 genesis refresh — HexaKit

**Date:** 2026-06-17

Post-eviction refresh of genesis governance docs after Waves 2–7 domain crate relocations.

## Evicted paths (workspace `exclude`)

| Path | Canonical owner |
|------|-----------------|
| `crates/settly` | phenotype-config |
| `Traceon/` | PhenoObservability (`tracingkit`) |
| `crates/stashly` | phenoShared (`stashly`) |
| `Metron/` | PhenoObservability (`metrickit`) |
| `agileplus/` | AgilePlus |

HexaKit remains **genesis-only** per charter; stubs + `MIGRATED.md` retained until archive-delete boundary.
