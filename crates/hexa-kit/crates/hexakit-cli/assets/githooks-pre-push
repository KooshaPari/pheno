#!/usr/bin/env bash
# Placeholder pre-push hook — replace with TestingKit-published bundle.
# Install: git config core.hooksPath .githooks
set -u
echo "[hooks] placeholder pre-push — replace from TestingKit"
exit 0
