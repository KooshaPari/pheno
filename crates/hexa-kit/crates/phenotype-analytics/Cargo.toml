[package]
name = "phenotype-analytics"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Analytics framework for the Phenotype ecosystem (stub — see MIGRATED.md)"

[lib]
path = "src/lib.rs"
