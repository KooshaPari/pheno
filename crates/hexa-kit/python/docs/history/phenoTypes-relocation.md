# Transitional relocation

`python/pheno-types` → canonical home is **[phenotype-types](https://github.com/KooshaPari/phenotype-types)**.

**Status (2026-06-16):** DISPOSITION step complete. HexaKit retains a stub pointer
(`README.md`, `MIGRATED.md`, deprecated `pyproject.toml`) until downstream references
are cleared and the directory is removed.

Install types from the canonical repo:

```bash
pip install git+https://github.com/KooshaPari/phenotype-types.git
```
