name: Security Guard (Hooks)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "**"

permissions:
  contents: read

jobs:
  guard:
    uses: KooshaPari/phenoShared/.github/workflows/reusable/security-guard-hook-audit.yml@95d94d5d77015368ee802130bf4feaae0b3566db
