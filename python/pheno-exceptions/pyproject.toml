[project]
name = "pheno-exceptions"
version = "0.1.0"
description = "PhenoSDK exceptions module"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pheno_exceptions"]
