name: VitePress Pages
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
jobs:
  deploy:
    uses: KooshaPari/phenoShared/.github/workflows/vitepress-pages.yml@4b820a6d1eca7884e692e4ec4edf2b59732809cd
    with:
      concurrency_group: pheno-vitepress-pages
    permissions:
      contents: read
      pages: write
      id-token: write
