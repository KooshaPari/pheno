name: Rust Quality Gate
permissions: read-all

on:
  workflow_call:
    inputs:
      rust-version:
        description: 'Rust toolchain version'
        required: false
        default: 'stable'
        type: string
      workspace:
        description: 'Cargo workspace path'
        required: false
        default: '.'
        type: string
      setup-protoc:
        description: 'Install protoc compiler'
        required: false
        default: false
        type: boolean
      coverage-threshold:
        description: 'Minimum coverage percentage'
        required: false
        default: 85
        type: number
      test-command:
        description: 'Test command to run'
        required: false
        default: 'cargo test --workspace'
        type: string

permissions:
  contents: read

jobs:
  quality:
    name: Quality Checks
    runs-on: ubuntu-24.04
    steps:
      - uses: ./.github/actions/setup-env
        with:
          rust-version: ${{ inputs.rust-version }}
          rust-components: rustfmt,clippy
          setup-protoc: ${{ inputs.setup-protoc }}
          workspace: ${{ inputs.workspace }}

      - uses: ./.github/actions/run-tests
        with:
          test-command: ${{ inputs.test-command }}
          lint-command: cargo clippy --workspace -- -D warnings
          format-check: 'true'
          working-directory: ${{ inputs.workspace }}

  coverage:
    name: Coverage
    runs-on: ubuntu-24.04
    steps:
      - uses: ./.github/actions/setup-env
        with:
          rust-version: nightly
          rust-components: llvm-tools-preview
          setup-protoc: ${{ inputs.setup-protoc }}
          workspace: ${{ inputs.workspace }}

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked

      - name: Generate coverage
        working-directory: ${{ inputs.workspace }}
        run: |
          mkdir -p coverage
          cargo llvm-cov --workspace --lcov --output-path coverage/lcov.info --fail-under-lines ${{ inputs.coverage-threshold }}

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@e79a6962e0d4c0c17b229090214935d2e33f8354  # v6.0.1
        with:
          files: ${{ inputs.workspace }}/coverage/lcov.info
          flags: rust
          fail_ci_if_error: false

  security:
    name: Security Audit
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      checks: write
    steps:
      - uses: ./.github/actions/setup-env
        with:
          rust-version: stable
          workspace: ${{ inputs.workspace }}

      - uses: ./.github/actions/security-checks
        with:
          cargo-audit: 'true'
          cargo-deny: 'true'
          gitleaks: 'false'
          python-bandit: 'false'
          working-directory: ${{ inputs.workspace }}
