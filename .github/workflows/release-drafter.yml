name: Release Drafter

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  release-drafter:
    uses: KooshaPari/phenoShared/.github/workflows/reusable-release-drafter.yml@95d74795c77f7c554be2b5e42e8e3378cdab77bf # pinned from @ref; org SHA may shift on new commits
    permissions:
      contents: write
      pull-requests: write
